- defaults:
    link:
      relink: true
      force: true
      create: true
      glob: true

- link:

    # NixOS
    /etc/nixos/:
      glob: true
      path: nixos/*
      exclude: [ nixos/README.md ]